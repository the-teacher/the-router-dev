name: the_router_dev

services:
  app:
    image: node:22
    command: sleep infinity
    working_dir: /app
    volumes:
      - ./the_router:/app/the_router
      - ./router_test_app:/app/router_test_app
    ports:
      - 3000:3000
